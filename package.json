{
  "name": "poker-ai-android",
  "version": "1.0.0",
  "private": true,
  "description": "离线 6 人桌 AI 德州扑克（安卓，Capacitor 打包）",
  "scripts": {
    "test": "node tests/test.js && node tests/smoke.js",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
